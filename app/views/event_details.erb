<div class="center"> <h1 id="event_details_header">Got to <%= @id.capitalize %>'s <%= @event %> page!</h1></div>
<div class="divider"></div>
<div class="center"><h2>Total Overall Events: <%= @user.payloads.total_events(@event) %><br><br></h2></div>

<% @count = 0 %>
<table class="highlight card-panel white centered responsive-table">
       <thead>
         <tr class="blue-text text-darken-2">
             <th data-field="id">Hour</th>
             <th data-field="name">Amount</th>
             <th data-field="price">Total Count</th>
         </tr>
       </thead>

<% @user.payloads.hour_edited(@event).each do |k, v| %>
       <tbody>
         <tr class="blue-text text-darken-2">
           <td><%= k %></td>
           <td><%= v %></td>
           <td><%= @count += v  %></td>
         </tr>
        <% end %>
       </tbody>
     </table>
